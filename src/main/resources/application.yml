mybatis:
  type-aliases-package: com.tcp.iamlazy
  config-location: classpath:mybatis/mapper/mybatis-config.xml

spring:
  profiles:
    include:
      - private
  http:
    log-request-details: true
  jpa:
    show-sql: true

logging:
  level:
    web: TRACE
    org:
      springframework:
        web: TRACE

server:
  port: 8092


app:
  oauth2:
    authorizedRedirectUris:
      - http://localhost:8080/calendar # you should change it : this is just not a valid site address.